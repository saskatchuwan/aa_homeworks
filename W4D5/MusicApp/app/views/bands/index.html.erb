<h1>Bands Index</h1>

<ul>
  <% @bands.each do |band| %>
    <li>
      <a href='<%= band_url(band) %>'><%= band.name %></a>
    </li>
  <% end %>

</ul>

<a href='<%= new_band_url %>'>Register a new band!</a>